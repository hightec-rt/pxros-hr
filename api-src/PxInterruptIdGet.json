{
  "name": {
    "key": "PxInterruptIdGet",
    "display": "PxInterruptIdGet()"
  },
  "synopsis": ["PxObjId_t PxInterruptIdGet (PxInterrupt_t Interrupt);"],
  "arguments": [
    {
      "name": "Interrupt",
      "description": "An Interrupt handle"
    }
  ],
  "retValues": ["Object ID of the object handle Interrupt"],
  "appliesTo": [
    {
      "ARM-CMX": ["1.0.0"],
      "TC23": ["8.2.0"]
    }
  ],
  "description": {
    "short": "Return the object ID of the object handle Interrupt",
    "long": [
      {
        "type": "PP",
        "text": "PxInterruptIdGet returns the object ID of the object handle Interrupt."
      }
    ]
  },
  "usage": [
    "#include \"pxdef.h\"",
    "",
    "PxObjId_t objid = PxInterruptIdGet(Interrupt);",
    ""
  ]
}
