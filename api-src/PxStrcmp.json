{
    "name": {
      "key": "PxStrcmp",
      "display": "PxStrcmp()"
    },
    "synopsis": ["PxInt_t PxStrcmp(char const *s, char const *s2);"],
    "arguments": [
      {
        "name": "s",
        "description": "First string to compare"
      },
      {
        "name": "s2",
        "description": "Second string to compare"
      }
    ],
    "retValues": [
        "0 if both strings are equal",
        "1 if s1 is lexicographically greater then s2",
        "-1 if s2 is lexicographically greater then s1"
    ],
    "appliesTo": [
      {
        "ARM-CMX": ["1.0.0"],
        "TC23": ["8.2.0"]
      }
    ],
    "description": {
      "short": "Compare two strings",
      "long": [
        {
          "type": "PP",
          "text": "PxStrcmp compare two strings. It returns \"0\", if they are equal."
        }
      ]
    },
    "usage": [
      "#include \"pxdef.h\"",
      "",
      "PxInt_t comp = PxStrcmp(str1, str2);",
      ""
    ]
  }
