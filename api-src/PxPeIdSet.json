{
  "name": {
    "key": "PxPeIdSet",
    "display": "PxPeIdSet()"
  },
  "synopsis": ["PxPe_t PxPeIdSet (PxObjId_t id);"],
  "arguments": [
    {
      "name": "id",
      "description": "The object id"
    }
  ],
  "retValues": ["Pe handle"],
  "appliesTo": [
    {
      "ARM-CMX": ["1.0.0"],
      "TC23": ["8.2.0"]
    }
  ],
  "description": {
    "short": "Return an object handle which holds id as the object ID",
    "long": [
      {
        "type": "PP",
        "text": "PxPeIdSet returns an object handle which holds id as the object ID."
      }
    ]
  },
  "usage": ["#include \"pxdef.h\"", "", "PxPe_t id = PxPeIdSet(oid);", ""]
}
