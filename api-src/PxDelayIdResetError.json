{
  "name": {
    "key": "PxDelayIdResetError",
    "display": "PxDelayIdResetError()"
  },
  "synopsis": ["PxError_t PxDelayIdResetError (PxDelay_t *Delay);"],
  "arguments": [
    {
      "name": "Delay",
      "description": "Pointer to the Delay handle"
    }
  ],
  "retValues": ["The last error of the object handle Delay"],
  "appliesTo": [
    {
      "ARM-CMX": ["1.0.0"],
      "TC23": ["8.2.0"]
    }
  ],
  "description": {
    "short": "Reset the last error of the object handle Delay",
    "long": [
      {
        "type": "PP",
        "text": "PxDelayIdResetError resets the last error of the object handle Delay"
      }
    ]
  },
  "usage": [
    "#include \"pxdef.h\"",
    "",
    "PxError_t err = PxDelayIdResetError(Delay);",
    ""
  ]
}
